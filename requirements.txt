# =============================================================================
# Sentinel-2 Spectral Index Pipeline — Requirements
# Notebook: sentinel2_pipeline.ipynb
# Python: >= 3.10  (tested on 3.11)
# =============================================================================

# --- STAC / Planetary Computer access ---
planetary-computer>=1.0.0      # Signs Planetary Computer asset URLs
pystac-client>=0.7.0           # STAC catalog search
stackstac>=0.5.1               # Lazy STAC -> xarray stacking
xarray>=2023.1.0               # Array model returned by stackstac
dask[array]>=2023.1.0          # Lazy/chunked array backend for stackstac

# --- Geospatial & Raster ---
rasterio>=1.3.0                # Raster I/O, mask, merge, MemoryFile
affine>=2.4.0                  # Affine transforms
geopandas>=0.14.0              # Vector GIS / AOI handling
shapely>=2.0.0                 # Geometry
folium>=0.16.0                 # Interactive maps

# --- Scientific Stack ---
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0                  # ndimage.zoom (band resampling)
matplotlib>=3.7.0
